export const paymentPolicy = { enabled: true, rail: "x402 paid seller risk API for platforms and agents", price: "$0.001 USDC", network: 'stellar:testnet', asset: 'USDC SAC testnet', facilitator: "https://channels.openzeppelin.com/x402/testnet", settlement: "TiwalaMarket ID exposes a paid machine-readable API. Agents receive HTTP 402 payment requirements, pay in USDC on Stellar, then get the verified data response." };
export function buildPaymentRequirement(){ if (!paymentPolicy.enabled) return { enabled: false, message: 'Agentic payments are documented but disabled for this MVP.' }; return { enabled: true, paymentRequired: { status: 402, scheme: 'exact', x402Version: 2, network: paymentPolicy.network, price: paymentPolicy.price, asset: paymentPolicy.asset, facilitator: paymentPolicy.facilitator, description: paymentPolicy.rail + ' - ' + paymentPolicy.price } }; }
